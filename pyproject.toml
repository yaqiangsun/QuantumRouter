[project]
name = "quantumrouter"
version = "0.0.1"
description = "Unified interface for quantum cloud providers. One API, any backend."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
